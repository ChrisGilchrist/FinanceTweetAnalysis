name: StockTwitsCSV
language: python
variables:
  - name: output
    inputType: OutputTopic
    description: Name of the output topic to write into
    defaultValue: messages4
    required: true
dockerfile: dockerfile
runEntryPoint: main.py
defaultFile: main.py
